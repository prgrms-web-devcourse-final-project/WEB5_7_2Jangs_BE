# 생성 Saga 로컬 성능 측정

작성일: 2026-09-04. 측정 대상은 커밋 생성이다. 스테이징 서버에는 요청하지 않았다.

## 판정

ID 목록을 자식 행 대신 JSON 한 컬럼에 저장하고 중복 조회를 제거한 뒤, 최적화 전 Saga 대비 중앙값이 50블록에서 27.0%, 200블록에서 51.3% 줄었다.

Saga를 적용하기 전 기준 코드보다는 중앙값이 각각 5.72ms, 5.27ms 늘었다. 사전 작업 기록과 완료 처리를 추가한 비용이 남아 있다. 현재 표본에서는 로컬 단일 요청 비용을 줄였다고 판단할 수 있지만 운영 부하에서도 괜찮다고 단정할 근거는 아니다.

## 조건

비교 대상은 기준 코드 `78c3cb3`, 최적화 전 Saga, 최적화 후 작업 트리다. 결과 폴더의 `staging`은 기준 코드의 이름이며 실행 서버는 모두 로컬이다.

각 버전에서 50블록과 200블록을 각각 한 차례 측정했다. 한 차례는 1 VU, 워밍업 3회, 본 측정 20회다. 총 본 측정 120회와 워밍업 18회가 성공했다. 로그인·문서 생성·그래프 조회는 setup으로 제외했다. 매 요청은 새 블록과 새 Idempotency-Key를 사용하므로 완료 결과 재사용 속도를 측정한 것이 아니다.

공용 블록 생성기와 `perf/create/commit/commit_create_benchmark.js`를 사용했다. 처음 요청별 2회만 실행했던 탐색 결과는 표본이 작아 최종 비교에서 제외했다. 추가 성능 실행 없이 기존 raw 파일의 `commit_create_ms` 값을 다시 확인해 이 보고서에 옮겼다.

## HTTP 응답 시간

단위는 ms다. 표는 소수 셋째 자리로 반올림했다.

| 블록 수 | 버전 | 표본 수 | 평균 | 중앙값 | p90 | p95 |
| --- | --- | --- | --- | --- | --- | --- |
| 50 | 기준 코드 | 20 | 20.139 | 18.662 | 24.464 | 26.142 |
| 50 | 최적화 전 Saga | 20 | 34.134 | 33.405 | 37.938 | 39.097 |
| 50 | 최적화 후 Saga | 20 | 24.690 | 24.387 | 29.339 | 31.483 |
| 200 | 기준 코드 | 20 | 21.169 | 20.706 | 22.010 | 26.056 |
| 200 | 최적화 전 Saga | 20 | 54.026 | 53.327 | 57.404 | 58.451 |
| 200 | 최적화 후 Saga | 20 | 26.544 | 25.978 | 28.772 | 29.307 |

| 블록 수 | 최적화 전 대비 중앙값 감소 | 최적화 전 대비 p90 감소 | 기준 코드 대비 중앙값 증가 | 기준 코드 대비 p90 증가 |
| --- | --- | --- | --- | --- |
| 50 | 27.0% | 22.7% | 30.7% | 19.9% |
| 200 | 51.3% | 49.9% | 25.5% | 30.7% |

감소율은 `(최적화 전 - 최적화 후) / 최적화 전`, 증가율은 `(최적화 후 - 기준) / 기준`으로 계산했다. 백분율은 반올림 전 값을 사용했다.

## DB 기록으로 확인한 차이

최적화 전·후 Saga에서 각각 작업 행 48개를 확인했다. 커밋 46개와 setup에서 만든 문서 2개이며 모두 COMPLETED였다.

최적화 전에는 커밋 46건의 블록 ID 5,750개와 CommitBlockSequence ID 46개가 총 5,796개 자식 행으로 저장됐다. 최적화 후에는 해당 자식 테이블이 없고 JSON의 블록 배열 길이 50인 작업 23개와 길이 200인 작업 23개가 기록됐다. 23개에는 워밍업 3회가 포함된다.

이번 최적화는 JSON 전환과 정상 시작 경로의 중복 조회 제거를 함께 적용했다. 두 변경을 따로 측정하지 않았으므로 개선분을 각각 몇 ms라고 나눠 말할 수는 없다.

## 근거 파일

모두 저장소 루트 기준 경로다. raw 결과는 기본적으로 Git에서 제외된다.

| 버전 | 50블록 | 200블록 |
| --- | --- | --- |
| 기준 코드 | perf/create/commit/results/staging/blocks-50/run-2/summary.json | perf/create/commit/results/staging/blocks-200/run-2/summary.json |
| 최적화 전 | perf/create/commit/results/preoptimized/blocks-50/run-1/summary.json | perf/create/commit/results/preoptimized/blocks-200/run-1/summary.json |
| 최적화 후 | perf/create/commit/results/optimized/blocks-50/run-1/summary.json | perf/create/commit/results/optimized/blocks-200/run-1/summary.json |

실행 조건과 데이터 정리는 [시나리오 README](../../perf/create/commit/README.md), 정합성 구조와 수용한 위험은 [설계 기록](../superpowers/specs/2026-09-04-mongo-create-durable-saga-design.md)을 참고한다.

## 해석 범위

20개 표본에서는 중앙값과 p90을 우선 보고 p95는 참고값으로만 본다. 무작위 순서의 반복 라운드나 신뢰구간 검증을 하지 않아 실행 순서·JIT·캐시·로컬 자원 경쟁의 영향을 분리하지 못했다. 동일 프로세스 내 순차 요청이므로 표본이 서로 독립이라고 가정하지 않는다.

동시 부하, 장시간 실행, 운영 데이터 크기, 보상 적체, MySQL 장애 상황의 처리량은 측정하지 않았다. 문서·브랜치·병합 생성 성능에도 이 수치를 그대로 적용하지 않는다. 복구 Worker와 지연된 요청의 P1 경합은 별도 문제이며 이 응답 시간 측정으로 안전성을 입증하지 않는다.
